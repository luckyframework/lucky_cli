name: Lucky CLI Weekly CI

on:
  schedule:
    - cron: "0 1 * * MON"
  workflow_dispatch:

jobs:
  integration-specs:
    uses: ./.github/workflows/reusable-earthly.yml
    with:
      earthly-cmd: +gh-action-weekly
